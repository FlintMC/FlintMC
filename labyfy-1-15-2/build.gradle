group 'net.labyfy'

labyfy{
}

jar{
    enabled = false
}

dependencies {
    project.parent.subprojects.each{ subProject->
        subProject.configurations.each{configurationRef ->
            labyfy project(path: subProject.path, configuration: configurationRef.name)
        }
    }
}
