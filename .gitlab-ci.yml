eval:
  stage: build
  script: bash eval.sh

get-mojang-libs:
  stage: test
  script: cat gradle.build && sh gradlew Labyfy:versioned[download-libraris]
  
build:
  stage: deploy
  script: sh gradlew build